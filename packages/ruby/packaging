# Available variables
# $BOSH_COMPILE_TARGET - where this package & spec'd source files are available
# $BOSH_INSTALL_TARGET - where you copy/install files to be included in package

set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Detect # of CPUs so make jobs can be parallelized
CPUS=$(grep -c ^processor /proc/cpuinfo)

export HOME=/var/vcap
export LIBYAML_PATH=/var/vcap/packages/libyaml
export ZLIB_PATH=/var/vcap/packages/zlib

# compile and install ruby
echo "Compiling and installing ruby..."

mkdir ruby/ruby-extracted && tar xfz ruby/ruby.tar.gz -C ruby/ruby-extracted --strip-components 1
cd ruby/ruby-extracted
./configure --prefix=${BOSH_INSTALL_TARGET} --enable-shared --disable-install-doc --with-opt-dir=$LIBYAML_PATH --with-opt-dir=$ZLIB_PATH
make -j${CPUS} && make install
echo "Finished installing ruby."
cd ..

# compile and install rubygems
echo "Installing rubygems..."
mkdir rubygems/rubygems-extracted && tar xfz rubygems/rubygems.tar.gz -C rubygems/rubygems-extracted --strip-components 1
rubygems/rubygems-extracted
${BOSH_INSTALL_TARGET}/bin/ruby setup.rb
if [[ $? != 0 ]] ; then
  echo "Cannot install rubygems"
  exit 1
fi
echo "Finished installing rubygems."
cd ..

# install the bundler version
mkdir bundler/bundler-extracted && tar xfz bundler/bundler.tar.gz -C bundler/bundler-extracted --strip-components 1
bundler/bundler-extracted
${BOSH_INSTALL_TARGET}/bin/gem build bundler.gemspec
${BOSH_INSTALL_TARGET}/bin/gem install *.gem --no-ri --no-rdoc
cd ..