<% if p('a9s_ssl_gateway.health.enabled') %>
server {
  listen <%= p('a9s_ssl_gateway.health.port') || 8080 %>;
  server_name _;

  location <%= p('a9s_ssl_gateway.health.path') || "/health" %> {
    return 200 "OK\n";
  }
}
<% end %>
