defaults: &defaults
  user: '<%= p('vhost_worker.user') %>'
  pass: '<%= p('vhost_worker.password') %>'
  host: '<%= p('vhost_worker.host') %>'
  vhost: /

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
